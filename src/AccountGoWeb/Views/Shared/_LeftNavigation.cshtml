<nav class="bg-gray-800 text-white w-64 h-full fixed top-0 left-0 overflow-auto">
    <div class="flex items-center justify-center border-b border-gray-700 p-4">
        <img src="~/images/gbd/logo.png" alt="Logo" class="w-12 h-12" />
        <div class="text-xl font-bold ml-2">Good Deed Books</div>
    </div>

    <ul class="py-4">

        <li class="px-4">
            <a href="#" class="flex items-center p-3 hover:bg-gray-700 rounded-lg transition-colors">
                <i class="fa fa-dashboard mr-2"></i>
                <span>Dashboard</span>
            </a>
        </li>

        <li class="px-4 relative parent">
            <button
                class="submenu-btn flex items-center justify-between w-full p-3 hover:bg-gray-700 rounded-lg transition-colors">
                <div class="flex items-center">
                    <i class="fa fa-industry mr-2"></i>
                    <span>Accounts Receivable</span>
                </div>
                <i class="fa fa-caret-down transition-transform duration-200"></i>
            </button>
            <ul class="child transition-all duration-300 pl-4 hidden">
                <li>
                    <a href="#" class="flex items-center p-3 hover:bg-gray-700 rounded-lg transition-colors">
                        Sales Quotations
                    </a>
                </li>
                <li>
                    <a href="#" class="flex items-center p-3 hover:bg-gray-700 rounded-lg transition-colors">
                        Sales Orders
                    </a>
                </li>
                <li>
                    <a href="#" class="flex items-center p-3 hover:bg-gray-700 rounded-lg transition-colors">
                        Sales Receipts
                    </a>
                </li>
            </ul>
        </li>

        <!-- 其他菜单项可以按照相同模式添加 -->
    </ul>
</nav>

<style>
    /* 子菜单展开/收起的过渡效果 */
    .parent:hover .child {
        display: block;
        opacity: 1;
        visibility: visible;
    }

    .child {
        opacity: 0;
        visibility: hidden;
        transition: all 0.3s ease-in-out;
    }

    /* 箭头旋转效果 */
    .submenu-btn.active .fa-caret-down {
        transform: rotate(180deg);
    }
</style>

<script>
    document.addEventListener('DOMContentLoaded', function () {
        const submenuBtns = document.querySelectorAll('.submenu-btn');

        submenuBtns.forEach(btn => {
            btn.addEventListener('click', function () {
                this.classList.toggle('active');
                const submenu = this.nextElementSibling;
                submenu.classList.toggle('hidden');
                submenuBtns.forEach(otherBtn => {
                    if (otherBtn !== this) {
                        otherBtn.classList.remove('active');
                        otherBtn.nextElementSibling.classList.add('hidden');
                    }
                });
            });
        });
    });
</script>