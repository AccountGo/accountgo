@model ICollection<Services.Financial.BalanceSheet>
@{
    ViewBag.Title = "BalanceSheet";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div style="text-align: center">
    <h3>Balance Sheet</h3>
</div>
<div>
    <table class="table">
        <tr style="font-weight: bold;">
            <td>Account Code</td>
            <td>Account Name</td>
            <td style="text-align: right">Amount</td>
        </tr>
        @foreach (var asset in Model.Where(a => a.AccountClassId == 1))
        {
            <tr>
                <td>@asset.AccountCode</td>
                <td>@asset.AccountName</td>
                <td style="text-align: right">@asset.Amount</td>
            </tr>
        }
        <tr style="font-weight: bold;">
            <td colspan="2">Total Assets</td>
            <td style="text-align: right">@Model.Where(a => a.AccountClassId == 1).Sum(a => a.Amount)</td>
        </tr>
    </table>
</div>
<div style="clear: both">
    <table class="table">
        <tr style="font-weight: bold;">
            <td>Account Code</td>
            <td>Account Namce</td>
            <td style="text-align: right">Amount</td>
        </tr>
        @foreach (var asset in Model.Where(a => a.AccountClassId == 2))
        {
            <tr>
                <td>@asset.AccountCode</td>
                <td>@asset.AccountName</td>
                <td style="text-align: right">@asset.Amount</td>
            </tr>
        }
        <tr style="font-weight: bold;">
            <td colspan="2">Total Liabilities</td>
            <td style="text-align: right">@Model.Where(a => a.AccountClassId == 2).Sum(a => a.Amount)</td>
        </tr>
    </table>
</div>
<div style="clear: both">
    <table class="table">
        <tr style="font-weight: bold;">
            <td>Account Code</td>
            <td>Account Namce</td>
            <td style="text-align: right">Amount</td>
        </tr>
        @foreach (var asset in Model.Where(a => a.AccountClassId == 3))
        {
            <tr>
                <td>@asset.AccountCode</td>
                <td>@asset.AccountName</td>
                <td style="text-align: right">@asset.Amount</td>
            </tr>
        }
        <tr style="font-weight: bold;">
            <td colspan="2">Total Equities</td>
            <td style="text-align: right">@Model.Where(a => a.AccountClassId == 3).Sum(a => a.Amount)</td>
        </tr>
    </table>
</div>

